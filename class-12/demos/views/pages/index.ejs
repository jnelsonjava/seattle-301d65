<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  
  <%- include('../layout/header') %>

  <h1>My Todos</h1>

  <ul>
    <% todos.forEach(todo => { %>
      <li>
        <h2>
          <a href="/tasks/1"><%= todo.task %> - <%= todo.category %></a>
        </h2>
        <p>Due <%= new Date(todo.deadline).toDateString() %> <%= todo.repeats ? '(repeats)' : '' %></p>
        <p><%= todo.criteria %></p>
      </li>
    <% }) %>
    
  </ul>
</body>
</html>